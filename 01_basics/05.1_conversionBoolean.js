// converson form to number to boolean

let isLoggedIn = ""; // put 1 , 0 , "preet"

let bolIsLoggedIn = Boolean(isLoggedIn);

console.log(bolIsLoggedIn); // it show false

// case -2

let isLoggedIn0 = "preet"; // put 1 , 0 , "preet"

let bolIsLoggedIn0 = Boolean(isLoggedIn0);

console.log(bolIsLoggedIn0); // it show ture

// for 1 true and 0 - falsed

