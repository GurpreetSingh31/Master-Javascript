
// go  to  browser and copy the below pradigm and check all the property and  methods
console.log(Math);  

console.log(Math.abs(-5));  // abs convert the neg numb to pos numb /
console.log(Math.round(6.87));  // used to round off the approximate value


console.log(Math.ceil(4.2)); // use in rounding off but choose the bigger number
console.log(Math.floor(4.2)); // use in rounding off but choose the bigger number


console.log(Math.min(3 , 5, 6 ,2)); // to find the min numb in an array
console.log(Math.max(3 , 5, 6 ,2)); // to find the max numb in arrary

// to use the random number and always it's value lie to 0 to  1 

console.log(Math.random()); // random value form 0 to 1 

console.log((Math.random()*10 )+ 1 ); // any random value will be 1 to 10 
console.log(Math.floor((Math.random()*10 )+ 1 )); // any random value but wrapp up to smalles value


//
const min = 10 
const max  = 20 
//  so the minumem value is 10 an max value is 20 
console.log(Math.floor(Math.random()* (max - min)+1+min));


// for my crazy stufff

